<h1>Books</h1>

<table>

  <thead>
    <th>Title</th>
    <th>Body</th>
    <th colspan="3"></th>
  </thead>
<% @books.each do |book| %>
  <tbody>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td>
      <%= link_to "Show", book_path(book.id) %>
      <%= link_to "Edit", edit_book_path(book.id) %>
      <%= link_to "Destroy", book_path(book.id), method: :deleted, "data-confirm" => "Are you sure？" %>
    </td>
  </tbody>
  <% end %>
</table>


<h2 class="new-book">New Book</h2>

<%= form_with model: @book do |f| %>

<p>Title</p>
<%= f.text_field:title %>

<p>Body</p>
<%= f.text_area:body %>

<p>
<%= f.submit "Create Book" %>
</p>

<% end %>

<!----まずtableタグで加工
<!----見出しにはtheadタグ、その中にthタグ
<!----本体部分にはt bodyタグ、その中にtdタグ
それぞれの行はtheadタグ、tbodyタグにつける
